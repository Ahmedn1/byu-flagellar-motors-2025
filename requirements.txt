# Versions known to work on Linux + CUDA 12.1 (training).
# The submission notebook only needs torch + Pillow + numpy + pandas + scipy at runtime
# (segmentation_models_pytorch is needed only for training, not inference).

torch==2.5.1
torchvision==0.20.1
segmentation-models-pytorch==0.5.0
numpy>=1.26
pandas>=2.2
scipy>=1.13
Pillow>=10
scikit-learn>=1.4
tqdm>=4.66
